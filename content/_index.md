---
title: Introduction
type: docs
---

# Selamat Datang

```js
(()=>{
  try {
    const say = console.log.bind(console)
    (clone_a_person === person) ?
      say("Depends, if clone by a reference or by value")
      : say("Hope that person doesn't become a robot")
  } catch (error) {
      const source ="https://stackoverflow.com/search?q=[js]+"
      window.open(`${source}${encodeURIComponent(error.message)}`)
  }
})()
```

